query QueryAllEvents {
    juster_event(order_by: {created_time: desc}) {
        creator_id
        created_time
        bets_close_time
        id
        currency_pair {
            symbol
        }
        measure_period
        target_dynamics
        pool_above_eq
        pool_below
    }
}
