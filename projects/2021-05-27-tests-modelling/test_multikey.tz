{ parameter (or (unit %default) (pair %increaseCounter (nat %amount) (nat %counterId))) ;
  storage (big_map (pair address nat) nat) ;
  code { UNPAIR ;
         IF_LEFT
           { DROP ; NIL operation ; PAIR }
           { DUP ;
             CDR ;
             SENDER ;
             PAIR ;
             DUP 3 ;
             DIG 2 ;
             CAR ;
             DIG 3 ;
             DUP 4 ;
             GET ;
             IF_NONE { PUSH nat 0 } {} ;
             ADD ;
             DIG 2 ;
             SWAP ;
             SOME ;
             SWAP ;
             UPDATE ;
             NIL operation ;
             PAIR } } }

