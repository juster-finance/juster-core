module Errors is {
    const disallowAmount : string = "Including tez using this entrypoint call is not allowed";
    const notManager : string = "Not a contract manager";
    const notProposedManager : string = "Not allowed to accept ownership";
}

